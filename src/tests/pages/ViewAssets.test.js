import { ViewAssets } from "../../pages";
import { render } from "../../test-utils";

describe("ViewAssets", () => {
  it("renders ViewAssets page correctly", () => {
    render(<ViewAssets />);
  });
});
