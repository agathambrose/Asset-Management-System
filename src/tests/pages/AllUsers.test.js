import { AllUsers } from "../../pages";
import { render } from "../../test-utils";

describe("All Users", () => {
  it("renders View Assets page correctly", () => {
    render(<AllUsers />);
  });
});
